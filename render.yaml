services:
  - type: web
    name: cuenta-corriente-mayorista
    env: node
    plan: starter
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: VITE_API_URL
        value: /api
